# Tauri updater public key. Not secret (it ships inside the binary) but kept
# out of git to avoid accidentally committing a half-filled placeholder.
# Copy this file to src-tauri/.env and paste either the raw .pub file content,
# the second line from the .pub file, or the base64 of the full .pub file.
TAURI_UPDATER_PUBKEY=
